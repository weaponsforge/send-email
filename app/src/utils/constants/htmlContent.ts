/**
 * Sample WYSIWYG HTML body content using inline CSS.
 * Adjust `@/utils/config/sanitizeHtml.js` to allow specific styles.
 * IMPORTANT:
 * - Use single-quotes to wrap inline styles.
 * - Wrap the WYSIWYG HTML content in double-quotes
 */

// eslint-disable-next-line quotes
const htmlContent = "<div style='width:100px; height:100px; border:5px solid blue; border-radius: 3px; padding: 8px; text-align: center; background-color: azure;'><h3>Hello, World</h3></div>"

export default htmlContent
